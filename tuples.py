# A tuple is a type of data structure where different values can be stored
# examples of a tuple is coordinates
# What makes tuples unique is that they are immutable(can't change anything)
# tuples are indexed starting at zero

coordinates = (2,4)
print(coordinates[1])